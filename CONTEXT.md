# PaperAI

PaperAI 是一个论文写作工作台：多个 Agent 引擎在同一份 Word 文档上协作写作，用户在界面里新建、修改、检查和导出论文文档。本文件只记录产品语言（术语），不记录实现。

## Language

**项目 (Project)**:
一个被 PaperAI 初始化过的目录：有固定的子目录结构，承载一个用户的论文写作工作。
_Avoid_: 工作区（这是底座 DSH 的叫法）、文件夹

**文档 (Document)**:
项目目录里由 PaperAI 追踪并记录版本的一份 Word 文件（开题报告、中期报告、学位论文等）。目录里未被追踪的文件不是文档。
_Avoid_: 论文、资源、Working DOCX、附件

**会话 (Session)**:
项目级的一次与 Agent 的对话，可以涉及项目里的任意文档，不隶属于某一份文档。
_Avoid_: 对话（口语可用，界面与文档中统一用"会话"）、聊天

**块 (Block)**:
文档界面里可以点选和编辑的最小单位：一个正文段落、一个标题、一张图连同图题、一张表连同表题、一个列表项，或一个空行。用户改块里的文字，结构性修改交给 Agent。
_Avoid_: 语义节点、节点、段落（泛指时）

**版本 (Version)**:
文档每一次被保存后的记录：谁改的、什么时候、改了什么；可以查看差异并恢复到任意一版。
_Avoid_: 提交、commit、快照、里程碑

**文档类型 (Document type)**:
一份文档在论文流程中的身份：开题报告、中期报告、学位论文、结题材料或其他。它决定文档使用模板里的哪一份格式，记在文档档案里。
_Avoid_: 角色、role、阶段

**模板 (Template)**:
一所学校（或用户自己）的一整套论文格式，按文档类型各含一份格式；属于全局配置（内置随产品提供，或由用户在设置中添加），不属于任何一个项目的内容。一个项目选用一套模板。
_Avoid_: 模板包、模板包成员、模板契约、模板资源、项目模板

**格式 (Format)**:
模板里对应某一种文档类型的那一份 Word 范式；一份文档最多绑定一份格式，绑定由文档类型自动决定，也可以由用户或 Agent 手动改。
_Avoid_: 模板成员、契约、要求集

**模板库 (Template library)**:
设置中管理的全部模板的集合，是项目选用模板时唯一的来源。
_Avoid_: 模板目录、模板包列表
