/**
 * PaperAI's product-profile patch bundle. Runtime behavior belongs to the
 * Cordis plugin rows inserted by `cordis.patch.yml`; this module has no API.
 * @module @paperai/bundle-web
 */

export {}
